import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Formula1 App',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
